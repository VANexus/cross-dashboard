# FlowMind 产品方案（AI-Native 跨境电商智能操作系统）

> 版本：v1.1 落地版 · 2026-09-02（v1.0 评审稿基础上，三项关键决策已拍板、P0 已编码并通过真实浏览器 E2E）
> 对应代码：`cross-dashboard`（Web AI-Native 微内核：cordis 内核 + pi 子代理 + 三层动态生成；P0 改动见 §6.4 落地清单）
> **三项已锁定决策（2026-09-02）**：① 楔子场景 = **TikTok Shop + 阿里国际站铺货**（非亚马逊精品优先）；② 终局为**对外 SaaS**，但**当前先自用**——数据模型预留多租户归属，不背计费/注册计费体系；③ **立即全面落地 P0 + 真实浏览器端到端测试**。
> 文档性质：**产品方案 / 功能简报级**（方向与 P0 已锁定；接口、工期、模型指标不由本文档承诺）
> 证据约定：【事实-代码】= 当前仓库可核验；【事实-外部】= 带来源的外部事实；【团队称】= 内部材料/自测主张，尚待外部验证；【产品决定】= 本方案主动选择；【待验证】= 假设，进入验证计划。

---

## 0. 一页决策简报

**这次要推动的决定**：新微内核技术架构已就绪（M0–M5 已落地、tsc/eslint/selftest 全绿），产品上必须回答五件事——**先为谁做、先打哪个闭环、做到什么程度、明确不做什么、按什么顺序推进**。

**一句话主张（产品决定）**：
> FlowMind 不做"又一个卖家数据工具"，而做**跨境操盘手的 AI 副驾——在数据所在的页面就地理解、给方案，经人确认后就地执行**；以 **TikTok Shop + 阿里国际站铺货**为楔子场景跑通"选品→Listing→配图→渠道上架"端到端闭环，再把可复用打法沉淀为动态工作流与技能包；终局对外 SaaS，当前先在自己团队自用跑顺。

**三条核心取舍（产品决定）**：
1. **不比数据精度，比编排与执行闭环。** 卖家精灵/Helium 10 的护城河是多年数据积累，且行业销量本就是估算（长尾词误差最高可达约 40%【事实-外部】），正面拼数据库是逆风仗；FlowMind 卖的是"看到→理解→决策→执行"链路的缩短。
2. **人始终在环中，Agent 不越权。** 对齐 Amazon 2025-09 把 Seller Assistant 升级为"reason, plan, and take action **with the seller's permission**"的行业范式【事实-外部】：只读可自动、草稿待确认、对外不可逆动作必须人工批准。
3. **先做深一条链，不铺开七块屏。** 现有 7 个工作空间是能力货架，但本期产品体验只主攻 1.5 条旅程，其余作为被 Agent 调用的能力，不追求每个页面都重做。

**本期范围（Now，已落地）**：1 个端到端"**TikTok·国际站铺货**"Agent 闭环 + L0/L1/L2 三级动作权限 + 数据/降级诚实标注 + 铺货 4 页面就地接入 + M4"保存为团队 SOP"最小版 + 4 个上线硬约束（RLS 归属预留/架构文档/迁移确认/成本闸）。
**明确 Non-goals**：不做 ERP 履约/财务/订单深度；不做广告资金的全自动投放；不做第三方开发者技能市场；**当前不做多租户商业化计费（只在数据层预留 workspace 归属，终局 SaaS 时再开启）**；不对外承诺提效百分比。

---

## 1. 产品定位

### 1.1 一句话价值主张
**"在你操盘的每个页面，都有一个懂跨境、会调工具、给方案并经你同意就动手的副驾。"**

- 对用户：少切后台、少搬数据、少做重复操作，把精力留给选品判断和生意决策。
- 对产品形态：Agent 不是一个"打开去聊"的对话框，而是溶解进每个业务界面的智能层（沿用 `plan.md` 的"操作系统守护进程"理念）。

### 1.2 品类坐标（定位图，示意）
两条结构性坐标轴：
- 横轴：**单点数据工具 ——————→ 跨域执行编排**（给数据 vs 替你把事做完）
- 纵轴：**人驱动（人找功能）——————→ Agent 驱动（能力主动找人）**

| 象限代表 | 位置 | 机制特征 |
|---|---|---|
| 卖家精灵 / Helium 10 | 左·偏人驱动 | 强数据库 + 单点功能，人来串流程 |
| 领星 ERP / 店小秘 | 中左·人驱动 | 多平台流程电子化，仍以人操作为主 |
| Amazon Seller Assistant（2025-09 起） | 中右·Agent 驱动 | 单平台内主动建议、**经同意执行**【事实-外部】 |
| Agentative（TikTok Shop）、Pattern Pi（70+ 站点） | 右侧·Agent 驱动 | "卖家操作系统/自主执行引擎"，偏海外平台与大企业【事实-外部】 |
| **FlowMind（目标位置）** | **右侧·Agent 驱动，但人在环中** | **跨平台编排 + 就地执行 + 三层可生长动态产物 + 微内核可装配技能** |

> 定位图为基于公开机制的**示意**，非精确市场份额测量；坐标证据见附录。

### 1.3 三条结构性差异化（来自机制对比，非功能勾选）
1. **就地闭环而非聊天搬运（Inline-first）**：页面上下文（快照≤2KB/状态≤1KB/页面动作）自动注入，Agent 通过 `ui_action` 直接在当前页面执行并回传，消灭"在聊天里描述数据→AI 给段文字→自己回去操作"的反模式【事实-代码：`lib/kernel/plugins/page-context.ts`、`ui-actions.ts`】。
2. **三层动态产物，把一次性回答变成可沉淀资产**：对话内组件（即时）→ 动态工作流（团队 SOP，可重跑）→ 动态页面（常驻/可分享交付物）【事实-代码：M3/M4/M5】。竞品大多停在第一层"对话出结果"。
3. **微内核 + 技能装配 = 可生长**：新增业务能力 = 注册一个工具/工作空间/技能，框架零改动【事实-代码：`lib/workspaces/registry.ts`、`src/kernel/plugins/tool-registry.ts`】。这让"运营方法论"能像装插件一样长到系统里，是后续技能生态的地基（本期只做内部装配，不开放第三方）。

---

## 2. 用户与问题

### 2.1 目标用户分群（产品决定：主攻 S1）

| 分群 | 画像 | 核心 Job | 痛点强度 | 本期策略 |
|---|---|---|---|---|
| **S1 TikTok Shop + 阿里国际站铺货/精铺小团队操盘手（1–3 人，多平台）** | TikTok Shop + 阿里国际站为主、亚马逊并行，老板/运营一体，靠高频上新铺货起量 | 用最少的人把"选品—上架—内容—投放"跑起来，追求上新速度与不出错 | **最高**：切换后台、搬数据、重复刊登吞掉产能，铺货模式下"手速天花板"更致命 | **主攻，P0 围绕其设计（已锁定）** |
| S2 内容/铺货型运营 | 高频上架、公众号/短视频内容发布 | 批量、快速、不出错地生产与发布 | 高：线性重复多 | 第二闭环（内容发布旅程）承接 |
| S3 团队负责人/管理者 | 看任务、风险、Agent 活动遥测 | 知道"系统在干什么、干得怎样" | 中：要可控可审计 | P1 用在场层与监控满足，不单独做重 BI |

> 与原始作品方案一致：首个落地场景为跨境电商、聚焦中小卖家及独立站团队【事实-代码/文档：`docs/Rak FlowMind作品设计方案.md`】。

### 2.2 问题强度（JTBD，可证伪陈述）
**S1 操盘手在"日常多平台运营"这一触发场景下，要完成选品/上架/内容/投放任务，受到"平台碎片化 + 流程碎片化 + 数据孤岛"阻碍，当前靠人工切换多个后台与多个单点工具替代，可观察后果是大量工时耗在搬运与重复操作、决策延迟。**

外部信号（问题真实存在，非孤例）：
- 一个中等规模卖家日单 500–1500，仅"切换后台看订单"每天耗 3–4 个人工时；爆单时决策延迟被放大【事实-外部：CSDN 跨境老炮实录，2026-08】。
- 多平台报表靠人工下载，数据更新频率常为"天级/周级"，导致选品滞后、库存超卖/滞销、广告效率低【事实-外部：实在智能，2026-07】。
- 多平台刊登存在"手速天花板"，复制标题/处理图片/逐项填规格既慢又易错【事实-外部：邦阅网/搜狐，2026】。
- 行业侧解法正在从"工具"转向"Agent 操作系统"：Amazon、Pattern、Agentative 均在 2025-09 至 2026-05 间转向 agentic/自主执行【事实-外部】。

**判断**：问题频率高（每日）、时间成本可量化、用户已为替代方案付费（ERP/数据工具）、且行业范式正在迁移——问题强度足以支撑投入。【产品决定】

### 2.3 要消灭的反模式（沿用 `plan.md`，作为体验红线）
打开聊天抽屉 → 向 AI 描述"我的数据长什么样" → AI 给一段文字 → 用户自己回页面操作。**任何 P0 流程若仍以这种形态收尾，视为未完成。**

---

## 3. 现状盘点：技术资产 → 产品能力（事实-代码）

> 原则：只把仓库里确实存在的能力写成"已有"；按成熟度分级，避免把骨架当成成品。

### 3.1 能力货架（7 大工作空间 / 29+ 页面）【事实-代码：`lib/workspaces/manifests/*`】
概览指挥（仪表盘）｜市场洞察（情报中心、关键词趋势）｜内容工坊（内容创作、公众号端到端发布）｜上架运营（一键上架、生图 Skill 库、渠道账号、AI 上架工作流）｜增长工作流（能力中心、选品、AI 作图、AI 广告、库销比、竞品广告分析、视频本地化）｜运行监控（账号风险、Agent 管理、任务中心）｜系统（记忆、自进化、设置）。

### 3.2 Agent 与编排资产成熟度分级

| 能力 | 代码位置 | 成熟度 | 产品含义 |
|---|---|---|---|
| Cordis 微内核（生命周期/DI/事件，前后端同构） | `src/kernel`、`lib/kernel` | ✅ 可用（selftest 过） | 插件化地基，支撑技能装配 |
| 统一对话 SSE + 6 专属工具 + 12 业务工具 | `app/api/agent/chat`、`tool-registry`、`local-tools`/`mcp-tools` | ✅ 主链可用 | Agent 调业务的主干道 |
| 页面上下文 + UI 即工具（就地执行闭环） | `lib/kernel/plugins/page-context`、`ui-actions` | ✅ 7 页已接入 | "就地干活"的核心，需扩到 P0 全链路页面 |
| pi 深度子代理（LongCat + 4 桥接工具，关闭内置危险工具） | `src/kernel/plugins/pi-subagent` | 🟡 可用待打磨 | 承接长任务，稳定性/成本待压测 |
| 三层动态生成（组件/工作流/页面） | M3 `generated`、M4 `mastra.runSpec`、M5 `/p/[slug]` | 🟡 打通待产品化 | 差异化关键，缺留存/权限/管理规范 |
| 业务旅程（选品上架 4 步、内容发布） | `lib/journeys/manifests` | 🟡 2 条真实+骨架 | 楔子场景骨架已在，需做成 Agent 引导闭环 |
| MCP 重技能（趋势/长尾/营销生图，含降级） | `lib/mastra/tools/mcp-tools`、`lib/content/mcp-client` | 🟡 已通含缓存降级 | 重活外呼，需把 degraded 状态显性化 |
| 6 个增长工作流页 | `app/workflows/*` | 🟠 成熟度不一 | 作为被调能力，不在本期重做 UI |
| 记忆/自进化/Agent 管理 | `memory`、`evolution`、`agents` | 🟠 骨架/演示 | P1 之后再产品化 |

### 3.3 既有效果主张（严格标注）
作品方案称：可承接各环节 80%–95% 的重复性标准化工作、3–5 分钟完成全流程、市场分析周期"周→天"【**团队称**：`docs/Rak FlowMind作品设计方案.md`，属内部自测口径，无外部对照样本，**不得作为对外卖点，只能作为待验证的内部目标基线**】。

---

## 4. 产品方案总览：三层产品结构（产品决定）

```
┌─ L3 动态产物层：对话内组件(即时) → 动态工作流(团队SOP) → 动态页面(常驻资产)
├─ L2 Agent 智能层：就地智能(Inline) · 在场感知(Ambient) · 主动介入(Proactive，经确认)
└─ L1 业务能力层：7 工作空间 = 可被 Agent 调用的"技能货架"
```

- **L1 业务能力层**：不新增大量页面，把现有 7 空间沉淀为 Agent 可调用的标准化技能（工具 + 数据 + 页面动作）。
- **L2 Agent 智能层**：
  - *就地智能*：选中/提问即在当前页出结论卡与可执行动作（P0）；
  - *在场感知*：顶部状态 + 活动遥测，让用户知道 Agent 在做什么（P1，复用 `stores/agent-presence`）；
  - *主动介入*：风险/机会以"建议卡"出现，三态（建议→采纳→驳回），**采纳前不自动改数据**（P1）。
- **L3 动态产物层**：明确三层的留存与权限差异（产品规则，当前代码只实现了机制）：

| 产物层 | 生命周期 | 谁可见 | 是否落库 | 本期策略 |
|---|---|---|---|---|
| 对话内组件 M3 | 会话内 | 本人 | 否 | P0，主打即时分析 |
| 动态工作流 M4 | 长期、可重跑 | 团队 | `wf_workflow_specs` | P0 末/P1，做"保存为团队 SOP" |
| 动态页面 M5 | 长期、可分享 | 按分享范围 | `wf_page_specs` | P2，先内部可见，分享后置 |

### 4.1 运营方法论 → 可装配技能包（第二增长曲线的种子，本期只做内部）
把九类成熟运营方法论各封装为"工具集 + 提示词策略 + 生成式 UI 模板 + 验收标准"的技能包，注册进微内核即可被 Agent 调用：

| 技能包 | 对应能力 | 本期 |
|---|---|---|
| 电商选品 | product_research、关键词趋势/长尾 | **P0（楔子链路用到）** |
| 跨境商品信息优化（Listing） | listing_generate/infringement/category | **P0** |
| 创意设计（生图） | imaging_generate / image_generate | **P0** |
| 跨境广告 & 内容增长 | ad_analyze/optimize、内容脚本 | P1（只到建议/草稿） |
| 电商活动策划 | 节点方案生成（动态页面/工作流） | P2 |
| 全案营销、VOC/舆情调研、营销内容 | 情报、内容工坊、记忆 | P2 |
| 商品/竞品分析 | competitor_analyze | P1 |

> 产品含义：微内核让"加一个运营技能"变成装配而非开发新模块；这是相对单体 SaaS 的结构性优势，但**本期不做第三方开发者市场**（Non-goal），先把内部技能装配流程跑顺。

---

## 5. 楔子场景：TikTok·国际站铺货端到端闭环（P0，已落地）

### 5.1 为什么是它（优先级论证）
- **楔子锁定 TikTok Shop + 阿里国际站铺货（产品决定）**：铺货模式上新频次高、单 SKU 决策轻、重复刊登最重，正是"编排闭环替代手工搬运"价值最密集处；相比亚马逊精品（重合规/重单链接打磨），铺货更能放大 Agent 的速度优势，也与团队自身实际经营一致（先自用）。
- **最高频且跨工具最多**：天然横跨"市场洞察→上架运营→增长"三个工作空间，最能体现编排价值；
- **链路在代码里最完整**：TikTok 热词/长尾（MCP）→ Listing 生成 → 生图（MCP）→ 渠道账号（tiktok/instagram/alibaba），4 步旅程已注册为 featured【事实-代码：`lib/journeys/manifests/listing-launch.ts`，label="TikTok·国际站铺货旅程"】；
- **价值可被用户立刻感知**：一次对话产出"打什么词 + 一套 Listing + 配图 + 上架就绪状态"，对比手工跨 4 个工具，节省可体感。

### 5.2 闭环故事板（目标体验，产品决定；对应代码已落地）
1. 用户在「关键词趋势」页（平台=TikTok）选中一个上升词 → 就地浮出 Agent 操作条："以此词启动铺货上架"。
2. Agent 自动带页面上下文（当前词、平台、行业），生成：长尾词扩展 + 类目建议 + 初步机会判断（**标注数据来源与是否为缓存/降级**）。
3. 用户确认关键词 → Agent 生成标题/五点/搜索词，并跑侵权词检测，风险词就地标红、给替换建议（**草稿态，不自动写回**；对应 listing 页 `generateListingDraft` 等 L1 动作）。
4. 用户确认文案 → Agent 调生图技能产出主图/场景图候选（**生成素材，不自动发布**；image-skills 页 L1 动作）。
5. 汇总为一张"上架就绪卡"，列出渠道账号状态与待办；**真正的渠道上架动作（对外不可逆，如 `publishListingToAlibaba` 推送阿里国际站、`importChannelSession` 导入渠道登录凭证）为 L2，弹确认卡展示影响面，用户批准后才执行一次**。
6. 全程可"保存为团队 SOP（动态工作流 M4）"，下次换词一键重跑。

第二闭环（S2）：内容发布旅程——选题/素材 → 文案 → 合规检查 → 公众号端到端发布，发布动作同样需确认。

### 5.3 人在环中：三级动作权限模型（关键产品决定，对齐行业范式）
> 依据：Amazon 明确"act when authorized / always keeping sellers in control"【事实-外部】；资金、账号、对外发布属高风险不可逆，必须保留人类责任主体。

| 级别 | 动作类型 | 例子 | 默认策略 | 对应代码机制（已落地） |
|---|---|---|---|---|
| **L0 只读/建议** | 查询、分析、生成结论卡 | 看趋势、算库销比、`listChannelAccounts`、页面导航/高亮 | 自动执行，直接呈现 | `riskLevel:'L0'` + 只读工具 + render_component |
| **L1 草稿/本地可逆** | 生成草稿、页面内填充、高亮、筛选 | `generateListingDraft`、生图技能、填表、页面跳转（缺省级=L1） | 执行到"待确认"，用户一键采纳/改 | `riskLevel:'L1'` ui_action（本地可逆，自动执行） |
| **L2 对外/不可逆/资金** | 渠道上架/发布、导入凭证、广告改价改预算、删除、对外发送 | `publishListingToAlibaba`、`importChannelSession`、公众号发布、调整广告出价 | **必须挂起并弹确认卡，展示影响面，批准后才执行一次；取消以正常结果回传、不重试** | ui_action `riskLevel:'L2'` 门 + 抽屉确认卡（`agent-drawer.tsx`） |

产品规则：**模型不得自行判定某 L2 动作"已获授权"**；授权来自当次用户明确点击。会话级"本次同类不再询问"可作为便捷开关，但默认关闭、可随时撤销。

---

## 6. 版本范围与优先级

### 6.1 上线前置硬约束（先于一切功能，属安全/正确性）
> 决策口径：**当前先自用、终局对外 SaaS**。因此 RLS 不一刀切到 authenticated-only（会让自用 anon 链路直接失效），而是"先加归属列为多租户预留、保持自用可用，并把未来生产锁定脚本写进迁移注释"，待真正对外时一键收紧。

| 项 | 原状【事实-代码】 | P0 落地 | 状态 |
|---|---|---|---|
| spec 表多租户归属与 RLS | 全库统一 anon 开放（`00002` 对所有表 `USING(true) WITH CHECK(true)`），`00012` 建 spec 两表 | `00013_saas_groundwork.sql`：给 `wf_workflow_specs`/`wf_page_specs` 加 `workspace_id`(default 'default-workspace')、`created_by`(default 'self') + 工作空间索引 + updated_at 触发器；**保持 anon 开放不破坏自用**；文件内注释给出"上 Auth 后翻成 tenant_isolation + revoke anon"的生产锁定脚本 | 代码已写，**待在远端 Supabase 执行确认** |
| 架构文档脱节 | `ARCHITECTURE.md`/`AGENTS.md` 仍写旧 RAK/sql.js；`lib/rak` 已无引用（死代码） | 顶部补"2026-09 微内核现状"说明（见 §6.4）；`lib/rak` 归档不在本期删除范围 | 进行中 |
| 迁移状态未知 | 代码引用 `wf_*_specs`、`channel_accounts`，远端缺 00008/00012/00013，运行期报"表不存在" | 保留"表缺失降级为空"兜底（页面/E2E 不受影响）；**需在真实 Supabase 补执行 00008/00012/00013 并验证**（上线阻断确认项） | 待执行 |
| 密钥与成本 | 统一密钥走服务端；pi/对话长链路 token 消耗无硬闸 | chat 路由新增 `MAX_AGENT_STEPS=12` 步数控闸（原硬编码 10 提为命名常量并注释成本口径），超限即止、不无限烧 token；系统提示词要求 L2 发起前先说明对外影响 | 已落地（步数控件）；按会话/日 token 额度计量留 P1 |

### 6.2 P0 / P1 / P2（范围即取舍）

**P0（Now）——一个能演示、能自用的深闭环（本轮已落地，验证见 §6.4）**
- 新增：TikTok·国际站铺货 Agent 端到端闭环（5.2）；L0/L1/L2 三级动作权限模型（5.3）；数据来源/降级诚实标注；page-context/ui_action 覆盖到铺货 4 页面（keyword-trends/listing/image-skills/channels）；"保存为团队 SOP（M4）"最小版（`plan_workflow` 落 `wf_workflow_specs`，00013 补归属列）。
- 保留：现有 7 工作空间、12 工具、MCP 重技能、统一对话主链（不重写）。
- 修改：生成式 UI 组件按闭环需要补齐/校准；Agent 抽屉新增 L2 确认卡与风险徽标；chat 系统提示词改为 TikTok+国际站铺货口径并写入三级权限行为约束与数据诚实要求；铺货旅程文案与 agentHint。
- 移除/不做：本阶段不碰记忆/自进化的深度产品化，不新增独立重页面；不做多租户计费。

**P1（Next）——可复用、可在场、可主动**
- 第二闭环（内容发布）；动态工作流管理（列表/重跑/版本）；在场层（活动遥测、任务进度）；主动洞察建议卡（风险/机会，采纳-驳回）；竞品分析、广告**建议**（不到自动投放）；pi 长任务稳定性与成本压测。

**P2（Later）——生态与扩展（探索，不承诺时间）**
- 动态页面分享（M5，含分享权限）；内部技能装配后台与技能包（活动策划/全案营销/VOC 等）；更多平台接入；**对外 SaaS 的多租户隔离开启与计费（数据层归属列已由 00013 预留，需另立商业验证，不在本路线内承诺时间）**。

### 6.3 优先级理由（为什么现在 / 推迟了什么 / 改排序信号）
- **现在做 P0 闭环**：技术主链已通、链路最完整、价值最可感，是验证"Agent 副驾"假设的最小成本实验。
- **主动洞察/在场层推迟到 P1**：没有先建立"单闭环可信"，主动推送会放大不信任；先被动可靠，再主动。
- **技能市场/商业化推迟到 P2**：没有内部标杆闭环前开放生态是空中楼阁。
- **改排序触发器**：若目标用户实测显示"内容发布"比"铺货上架"更高频/更痛，则把第二闭环提前；若 MCP 趋势数据稳定性不达标，则 P0 先用缓存/手工导入兜底，不阻塞闭环体验。

### 6.4 P0 落地清单与验证记录（2026-09-02，事实-代码）

**代码改动**
- 风险分级内核：`lib/kernel/plugins/ui-actions.ts` 增 `ActionRiskLevel('L0'|'L1'|'L2')`、`RISK_META`、`riskLevelOf()`、动作定义增 `riskLevel?`（缺省 L1）/`confirmText?`、测试注册缝；`lib/agent/ui-actions.ts` 同步 re-export。
- 代理透传修复：`lib/kernel/plugins/page-context.ts` 页面动作包装代理补透传 `riskLevel/confirmText`（否则 L2 在包装层失效），上下文上报风险等级。
- L2 确认门：`components/agent/agent-drawer.tsx` 实现 L0/L1 直执行、L2 挂起弹确认卡（批准执行一次／取消以**正常结果**回传"用户取消未执行、不要重试"，避免模型当失败重试）、风险徽标、`safeAddToolResult`（toolCallId 不在消息树则跳过回传，根除控制台 `reading 'parts'` 报错）。
- chat 路由：`app/api/agent/chat/route.ts` 系统提示词改 TikTok+国际站铺货口径、写入完整三级权限行为约束与数据诚实要求、动作清单按 `- [Lx] id — desc` 注入、`MAX_AGENT_STEPS=12` 成本闸。
- 铺货 4 页就地接入：listing（含 L2 `publishListingToAlibaba`）、image-skills（3 个 L1）、channels（L0 `listChannelAccounts`/L1 `verifyChannelAccount`/L2 `importChannelSession`），keyword-trends 原已接入。
- 旅程：`lib/journeys/manifests/listing-launch.ts` label="TikTok·国际站铺货旅程"，4 步=TikTok 热词选品→Listing→主图素材→渠道上架，每步补 agentHint。
- 迁移：`supabase/migrations/00013_saas_groundwork.sql`（归属列+索引+触发器+注释化生产锁定脚本）。
- M4 最小用户可见版：新增 `app/api/agent/workflows`（GET 列表/POST 保存，保存前 zod+拓扑+工具白名单校验）与 `app/api/agent/workflows/[id]/run`（POST 重跑，复用 `mastra.runSpec`）；旅程执行视图新增 `components/journey/team-sop-panel.tsx`（"保存当前旅程为团队 SOP"+SOP 列表+重跑每步成败）；铺货 SOP 模板只编排 b2b_trends→b2b_longtail→listing_generate→listing_infringement→imaging_generate，**L2 渠道发布不进自动 SOP**。
- 生产构建修复：`app/p/[slug]/page.tsx` 把与 `cacheComponents` 冲突的 `export const dynamic='force-dynamic'` 改为 `connection()`（沿用 b2b island 约定）。

**验证记录（本机真实执行）**
- `bunx tsc --noEmit` exit 0；全部改动源文件 `eslint` exit 0。
- 新增真实浏览器 E2E `e2e/listing-launch-p0.spec.ts`（6 用例，chromium，经 `window.__agentUI` 直驱、不依赖 LLM）：4 页动作注册与风险分级、TikTok/国际站口径可见、**L2 不自动执行→取消不执行→再挂起→批准恰好执行一次**、L1 自动执行不弹门、带 pageerror 回归守卫；6/6 通过。
- M4 最小版 E2E `e2e/team-sop-m4.spec.ts`（2 用例，route 打桩隔离远端缺表）：保存 SOP→列表→重跑每步成败、无 SOP 空态；2/2 通过。
- 全量回归：全部 spec 通过（含修复后 `journeys.spec.ts` 14/14）；客户端导航进入 async SSR island 时 Next dev 会残留一个 hidden 旧子树，测试已只取可见旅程条（生产构建无此现象）。
- 生产构建：清理被 dev 写坏的 `.next` 后 `bun run build` 成功（Turbopack 编译 + TypeScript + 118 页生成，无告警）。
- **待办（上线阻断确认项）**：远端 Supabase 补执行 00008/00012/00013（M4 SOP 落库/重跑与 M5 动态页依赖这些表，当前缺表时 API 与 UI 均降级为空、不崩）。

---

## 7. Outcome-Driven Roadmap（Now / Next / Later）

> 无真实人力与排期承诺，采用 Now/Next/Later；阶段以"结果信号/退出条件"驱动，而非日期堆叠。

### 阶段 Now：跑通并自证一条链
- **目标结果**：自己团队（S1）能在一次连续对话+少量确认内，完成"TikTok 热词→Listing→配图→渠道上架就绪（含阿里国际站 L2 推送）"，且清楚每步数据来源与所需授权，先把闭环自用跑顺再谈对外。
- **当前问题**：能力分散在 7 块屏，Agent 能调但体验未串成闭环，动作权限无分级，数据降级状态用户不可见。
- **关键行动**：6.1 四个硬约束；5.2 闭环；5.3 权限；4 页面就地接入；M4 最小保存。
- **依赖与容量**：MCP 趋势/生图可用性、统一密钥额度、Supabase 迁移（确认状态见 6.1）。
- **结果信号（口径先行，见 §8）**：闭环完成率、人均周闭环数、步骤间人工搬运次数下降、确认动作的采纳/驳回比。
- **退出条件（进入 Next）**：≥5 名目标用户在无干预下走通闭环；P0 验收场景全过；硬约束清零。
- **重排触发器**：闭环完成率低源于数据不可用而非交互 → 先补数据接入；用户对 L2 确认普遍反感 → 调整为更细粒度授权而非取消。

### 阶段 Next：从"能用"到"可依赖、可复用"
- **目标结果**：团队能把高频打法沉淀为可重跑 SOP；Agent 状态透明、能在风险点主动提醒且不越权。
- **关键行动**：第二闭环、工作流管理、在场层、主动建议卡、广告/竞品建议、pi 稳定性。
- **退出条件**：至少 2 条旅程可日常复用；主动建议的采纳率达到内部设定的可接受区间（区间在 Now 阶段用基线定，不预设数值）。

### 阶段 Later：生长与扩展（探索）
- **目标结果**：验证"技能可装配"是否显著降低新业务上线成本；评估动态页面对外分享与多团队价值。
- **进入前提**：Next 阶段出现明确的复用/分享需求证据；否则保持探索、不投入重资源。

---

## 8. P0 核心需求卡（研发/测试可讨论）

> 每张卡沿"触发→系统行为→用户可见→异常恢复→依赖→验收"闭合。接口与性能指标由研发评估，本卡不代拟。

### 需求卡 A：选品上架 Agent 闭环
- **谁在何前提下触发**：S1 用户在关键词趋势页选中词并点击"启动选品上架"（或在抽屉表达等价意图）。
- **系统行为**：注入当前页上下文 → 依次调用 长尾/类目 → Listing 生成+侵权检测 → 生图，产出分步结论卡与最终"上架就绪卡"；每步标注数据来源/缓存/降级。
- **用户可见**：每步可展开看依据；草稿可就地编辑；L2 上架动作出现确认卡（影响面+批准/取消）。
- **异常恢复**：MCP 失败→用历史缓存并明示"降级"；生图失败→允许重试/跳过该步不阻断后续；用户取消→保留已生成草稿。
- **依赖**：chat SSE、page-context、listing/imaging/trends 工具、MCP、M4 保存。
- **验收**：
  - *成功*：给定一个词，无人工跨页搬运，连续产出 长尾+Listing（含侵权标红）+配图候选+就绪卡；保存为 SOP 后可对新词重跑。
  - *失败/边界*：MCP 不可用时全程不崩、明确降级标识与重试入口；中途刷新页面后能恢复或明确提示不可恢复的范围。

### 需求卡 B：三级动作权限与确认
- **系统行为**：每个 ui_action/工具声明 `riskLevel: L0/L1/L2`；L2 必须渲染确认卡，未批准不执行；记录采纳/驳回。
- **用户可见**：L0 直接出结果；L1 显示"草稿/待采纳"；L2 显示将影响的对象与 diff，批准后执行并给撤销/结果反馈。
- **异常恢复**：L2 执行失败→回滚本地草稿态并提示；重复点击→幂等，不重复发起对外动作。
- **验收**：
  - *成功*：渠道上架/发布/广告改价在无明确批准时绝不执行；批准后执行一次且有结果回执。
  - *边界*：模型返回"用户已授权"但本会话无点击记录时，仍判定未授权并再次请求确认（防越权）。

### 需求卡 C：动态工作流沉淀与复用（M4 最小版）
- **系统行为**：把一次闭环的步骤 spec 存入 `wf_workflow_specs`；支持列表查看、对新输入重跑（`runSpec` 拓扑执行，单步失败记录并继续、整体标 failed）。
- **用户可见**："保存为团队 SOP"按钮、SOP 列表、重跑进度与每步结果。
- **异常恢复**：spec 校验不过→拒绝保存并说明；某步失败→标出失败步、允许从该步重试。
- **验收**：*成功* 保存后重跑产出结构一致的结果；*边界* 引用了已下线工具的 SOP 给出明确缺失提示而非静默跳过。

### 需求卡 D：数据来源与降级诚实标注
- **背景**：行业数据多为估算，且系统已有 `degraded/warning/cache(mode,hit,ageS)` 字段【事实-代码】。
- **系统行为**：所有趋势/销量/竞品数据卡显示来源、更新时间、是否缓存/降级、估算口径说明。
- **验收**：*成功* 实时/缓存/降级三态在 UI 可区分；*边界* 无任何来源的数据显示"暂无数据"而非给看似精确的假数。

---

## 9. 指标与验证（口径先行，不承诺幅度）

> 基线未知，先定义采集口径；在拿到 Now 阶段基线前，不写提升百分比目标。

- **北极星候选（Now 末定一个）**：每周"Agent 协助完成的业务闭环数"（一个闭环=从意图到 L1 采纳/L2 批准完成）。
- **过程漏斗**：召唤 Agent → 生成方案 → 用户采纳/批准 → 闭环完成，逐级埋点，定位流失步。
- **健康度/信任**：L2 确认的批准率与驳回原因分布；降级触发率；单闭环平均模型成本（用于设用量上限）。
- **质化验证（Now 退出条件）**：≥5 名符合 S1 的真实卖家走查，观察"是否还需跨页人工搬运""是否信任并敢点批准"。
- 对外提效数字（如 80–95%、3–5 分钟【团队称】）必须在有对照样本后才可使用，否则仅作内部目标。

---

## 10. 风险与硬约束

| 风险 | 性质 | 应对（产品侧） |
|---|---|---|
| spec 表 anon 可读写 | 安全/合规，**上线阻断项** | 见 6.1，收紧 RLS + 归属隔离 |
| 行业数据为估算、可能误导决策 | 信任 | 需求卡 D 诚实标注；关键决策提示人工复核 |
| L2 误操作（上架/发布/花钱） | 不可逆、资金/账号 | 三级权限 + 确认卡 + 幂等 + 操作日志，责任留在人 |
| pi 长任务成本/稳定性 | 成本/体验 | 用量上限、超时降级、长任务进度可见（P1 压测） |
| 平台 ToS/合规（自动操作平台后台） | 合规 | 自动化限定在合规边界，敏感动作人工执行，不做规避平台风控的功能 |
| 文档债与死代码误导迭代 | 工程效率 | 更新架构文档、归档 `lib/rak` |
| 单团队资源有限铺不开 | 范围 | 严格 1.5 条旅程，其余作为被调能力 |
| 效果主张被当对外承诺 | 对外合规 | 未验证前不对外使用提效百分比 |

---

## 11. 决策记录与仍开放问题

**已拍板（2026-09-02，不再作为开放问题）**
1. ~~首个标杆更偏亚马逊精品还是 TikTok+国际站铺货？~~ → **锁定 TikTok Shop + 阿里国际站铺货优先**，亚马逊作为并行能力保留、不做楔子。
2. ~~最终自用还是对外 SaaS？~~ → **终局对外 SaaS、当前先自用**：数据层用 00013 预留 `workspace_id/created_by` 多租户归属，但本期不建注册/计费/租户管理，不背商业化包袱；对外前用迁移内注释脚本收紧 RLS。

**仍开放（仅保留会改变方向的少量问题）**
1. L2 动作的授权粒度：逐次确认 vs 会话内规则化授权，用户更接受哪种？——Now 阶段自用走查时用真实反馈定（默认逐次确认，"本次同类不再询问"默认关闭）。
2. 对外 SaaS 的租户隔离与计费形态（按席位/按动作量/按工作空间）——进入 Later、出现外部付费意向时再立商业验证，不在 Now 预设。
> 若暂不回复：按"TikTok+国际站铺货先行 + 先自用、预留多租户 + 逐次确认为默认"推进。

---

## 附录：证据来源（访问时间 2026-09-02）

**内部（事实-代码，仓库 `cross-dashboard`，P0 落地于 2026-09-02）**
- 微内核与插件：`src/kernel/*`、`lib/kernel/*`（风险分级见 `lib/kernel/plugins/ui-actions.ts`、上下文透传见 `page-context.ts`）；对话主链：`app/api/agent/chat/route.ts`（三级权限提示词 + `MAX_AGENT_STEPS`）；L2 确认门：`components/agent/agent-drawer.tsx`；三层动态生成：`components/agent/generated`、`src/kernel/plugins/mastra-engine.ts`、`app/p/[slug]`；工具：`lib/mastra/tools/{local,mcp}-tools.ts`；工作空间/旅程：`lib/workspaces/registry.ts`、`lib/journeys/manifests/listing-launch.ts`；铺货 4 页客户端：`app/b2b/{keyword-trends,listing,image-skills,channels}/*`；迁移：`supabase/migrations/00012_agent_specs.sql`、`00013_saas_groundwork.sql`；原始定位：`docs/Rak FlowMind作品设计方案.md`、`plan.md`。
- 本机验证（2026-09-02）：`tsc --noEmit` exit 0；改动文件 eslint exit 0；新增 `e2e/listing-launch-p0.spec.ts` 真实 chromium 6/6 通过；全量 Playwright 回归通过（journeys 14/14）。远端 Supabase 尚未应用 00008/00012/00013（运行期降级为空，待执行确认）。

**外部（事实-外部）**
- Amazon 官方：Seller Assistant 升级为经同意才行动的 agentic AI（2025-09-17）— https://www.aboutamazon.com/news/innovation-at-amazon/seller-assistant-agentic-ai
- Digital Commerce 360：Amazon agentic 工具与 200 万+卖家背景（2025-09-17）— https://www.digitalcommerce360.com/2025/09/17/amazon-launches-agentic-ai-tools-to-automate-seller-operations/
- Pattern Intelligence(Pi) 自主执行引擎、70+ 站点（2026-05-22）— https://stellagent.ai/insights/pattern-group-pi-ai-execution-engine
- Agentative：TikTok Shop "one OS"、60+ agents — https://agentative.ai/blog/agentative-vs-cruva-euka-dashboardly
- 卖家精灵/H10 对比与"销量为估算、长尾词误差最高约 40%"（创作者口径，2026-08）— 抖音跨境工具测评；Helium10 中文官网 — https://h10.com.cn/
- 多平台运营痛点（切换后台工时、数据孤岛、刊登效率）— CSDN(2026-08)、实在智能(2026-07)、邦阅网/搜狐(2026)。

> 外部创作者/厂商口径存在立场，已在正文降低结论强度；市场规模类数字因缺统一口径未采用。
