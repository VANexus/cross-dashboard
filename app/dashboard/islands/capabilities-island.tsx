import { DashboardCapabilities } from "../dashboard-capabilities";

/** Agent 能力中心：真实能力清单（业务/编排/记忆/组建），一键交棒主 Agent 编排。 */
export async function CapabilitiesIsland() {
  return <DashboardCapabilities />;
}
