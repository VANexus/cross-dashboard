import { WorkflowTopology } from "../workflow-topology";

/** 仪表盘「动态工作流可视化」：主 Agent 规划/执行过的动态 SOP（wf_workflow_specs）DAG 流水线 */
export function WorkflowTopologyIsland() {
  return <WorkflowTopology />;
}
